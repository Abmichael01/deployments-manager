{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Start App",
            "type": "shell",
            "command": "pnpm install && pnpm dev -- -p 3012",
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "isBackground": true,
            "problemMatcher": [],
            "presentation": {
                "group": "group 1"
            },
            "runOptions": {
                "runOn": "folderOpen"
            }
        }
    ]
}